---
import Layout from '../layouts/Layout.astro';
import { getEntry } from 'astro:content';

const post = await getEntry('novel', 'sample');
const { Content } = await post.render();
---

<Layout title="Welcome to Astro.">
	<main>
		<Content />
	</main>
</Layout>

